name: Deploy to Vercel

run-name: Deploy Vercel Backend

on:
  push:
    branches:
      - main
      - claude/**
    paths:
      - 'vercel-backend/**'
      - '.github/workflows/deploy-vercel.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'vercel-backend/**'
      - '.github/workflows/deploy-vercel.yml'

  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - production
          - preview
        default: 'preview'

# Prevent concurrent deployments to the same environment
concurrency:
  group: vercel-deploy-${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: false

# Minimal permissions following principle of least privilege
permissions:
  contents: read
  pull-requests: write  # Required for PR comments
  deployments: write    # Required for deployments

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: main

    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Vercel Secrets
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "❌ Error: VERCEL_TOKEN secret is not set!"
            echo "Please add it in: Settings → Secrets and variables → Actions"
            echo "See .github/VERCEL_SETUP.md for setup instructions"
            exit 1
          fi
          if [ -z "${{ secrets.VERCEL_ORG_ID }}" ]; then
            echo "❌ Error: VERCEL_ORG_ID secret is not set!"
            exit 1
          fi
          if [ -z "${{ secrets.VERCEL_PROJECT_ID }}" ]; then
            echo "❌ Error: VERCEL_PROJECT_ID secret is not set!"
            exit 1
          fi
          echo "✅ All Vercel secrets are configured"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # Note: npm cache disabled - no package-lock.json, only global vercel CLI install

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel Environment Information
        working-directory: ./vercel-backend
        run: vercel pull --yes --environment=${{ github.ref == 'refs/heads/main' && 'production' || 'preview' }} --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        working-directory: ./vercel-backend
        run: vercel build ${{ github.ref == 'refs/heads/main' && '--prod' || '' }} --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel
        id: deploy
        working-directory: ./vercel-backend
        run: |
          DEPLOYMENT_URL=$(vercel deploy --prebuilt ${{ github.ref == 'refs/heads/main' && '--prod' || '' }} --token=${{ secrets.VERCEL_TOKEN }})
          echo "url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
          echo "### 🚀 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Deployment URL:** $DEPLOYMENT_URL" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** ${{ github.ref == 'refs/heads/main' && 'Production' || 'Preview' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '### 🚀 Vercel Deployment\n\n**Preview URL:** ${{ steps.deploy.outputs.url }}\n\nThe backend has been deployed to Vercel preview environment.'
            })

    outputs:
      url: ${{ steps.deploy.outputs.url }}
