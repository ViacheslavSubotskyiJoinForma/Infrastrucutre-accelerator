terraform {
  backend "s3" {
    encrypt        = true
    bucket         = "{{ state_bucket }}"
    key            = "${var.env}/vpc/tf.state"
    dynamodb_table = "{{ dynamodb_table }}"
    region         = "{{ region }}"
  }
}
