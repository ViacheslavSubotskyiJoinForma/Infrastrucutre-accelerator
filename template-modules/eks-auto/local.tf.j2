locals {
  cluster_name = "${var.project_name}-${var.env}-eks"

  tags = merge(
    var.tags,
    {
      Project     = var.project_name
      Environment = var.env
      Component   = "eks-auto"
      ManagedBy   = "Terraform"
    }
  )
}
