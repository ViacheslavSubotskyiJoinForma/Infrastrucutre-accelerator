# MVP version uses local state backend
# For production use, configure S3 backend with state locking:
#
# terraform {
#   backend "s3" {
#     encrypt        = true
#     bucket         = "your-terraform-state-bucket"
#     key            = "${var.env}/rds/tf.state"
#     dynamodb_table = "your-terraform-locks-table"
#     region         = "{{ region }}"
#   }
# }
